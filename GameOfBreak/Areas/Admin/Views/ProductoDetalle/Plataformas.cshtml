@model GameOfBreak.Models.PaginationViewModel<GameOfBreak.Models.GoB.Plataforma>
@{
    ViewBag.Title = "Plataformas";

    var plataformas = Model.Items;
    var pager = Model.Pager;
}

<h2>Plataformas</h2>

@Html.ActionLink("Nueva Plataforma", "AgregarPlataforma", "ProductoDetalle", null, new { @class = "btn btn-default" })

<table class="table table-hover table-striped">
    <thead>
        <tr>
            <th>Desarrolladora</th>
            <th>Accion</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var plataforma in plataformas) {
            <tr>
                <td>@plataforma.Plataforma1</td>
                <td>

                    <div class="dropdown">
                        <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu2" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Accion
                            <span class="caret"></span>
                        </button>
                        <ul class="dropdown-menu" aria-labelledby="dropdownMenu2">
                            <li>@Html.ActionLink("Editar", "EditarPlataforma", "ProductoDetalle", new { idPlataforma = plataforma.ID_PLATAFORMA }, null)</li>
                        </ul>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>


<div class="row">

    <div class="col-sm-6">
        <nav aria-label="Page navigation">
            <ul class="pagination">

                @for (int i = pager.StartPage; i <= pager.EndPage; i++) {
                    <li class="@(i == pager.CurrentPage ? " active": ")" )">@Html.ActionLink(i.ToString(), "Plataformas", "ProductoDetalle", new { page = i, count = pager.TotalItems }, null)</li>
                }

            </ul>
        </nav>
    </div>

    <div class="col-sm-6">
        Total de registros obtenidos:
        <div class="badge">
            @pager.TotalItems
        </div>
    </div>

</div>